import { NextBusBoard } from "@/components/bus/next-bus-board";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Next Bus | StakeOS"
};

export default function BusPage() {
  return <NextBusBoard />;
}
